// select the collection of span tags from the page
// hide them immediately in the beginning
// add them back in using animation and a timer

// expected result
// letters will be brought in one at a time onto the page

console.log("main");